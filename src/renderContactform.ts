/**Update the contact form to match the contact data given*/
export default function renderContactForm(contactData: contactData) {
    contactData.name || '';
    contactData.phonenumber || '';
    contactData.email || '';
    contactData.address || '';
}
